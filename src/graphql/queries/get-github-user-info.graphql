query GetGithubUserInfo($searchQuery: String!){ 
  search(query:$searchQuery, type: USER, first: 5, ) {
    edges{
      node{
        ... on User {
          login
          name
          avatarUrl
          id
        }
        ... on Organization {
					login
          name
          avatarUrl
        }
      }
    }
  }
}